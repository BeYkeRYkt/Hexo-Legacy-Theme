<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Themes in the "Theme.AppCompat" family will contain an action bar by default.
     If Holo themes are available on the current platform version they will be used.
     A limited Holo-styled action bar will be provided on platform versions older
     than 3.0. (API 11)
     These theme declarations contain any version-independent specification. Items
     that need to vary based on platform version should be defined in the corresponding
     "Theme.Base" theme. -->

    <!-- Platform-independent theme providing an action bar in a dark-themed activity. -->
    <style name="Theme.AppCompat" parent="@android:style/Theme.Holo"/>

    <!-- Platform-independent theme providing an action bar in a light-themed activity. -->
    <style name="Theme.AppCompat.Light" parent="@android:style/Theme.Holo.Light"/>

    <!-- Platform-independent theme providing an action bar in a dark-themed activity. -->
    <style name="Theme.AppCompat.Light.DarkActionBar" parent="@android:style/Theme.Holo.Light.DarkActionBar"/>

    <style name="Theme.AppCompat.NoActionBar" parent="@android:style/Theme.Holo.NoActionBar">
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar" parent="@android:style/Theme.Holo.Light.NoActionBar">
    </style>

    <style name="Theme.AppCompat.DialogWhenLarge" parent="@android:style/Theme.Holo.DialogWhenLarge">
    </style>

    <style name="Theme.AppCompat.Light.DialogWhenLarge"
           parent="@android:style/Theme.Holo.Light.DialogWhenLarge">
    </style>

    <style name="Theme.AppCompat.Dialog" parent="@android:style/Theme.Holo.Dialog"/>

    <style name="Theme.AppCompat.Light.Dialog" parent="@android:style/Theme.Holo.Light.Dialog"/>


    <!-- Material theme for alert dialog windows, which is used by the AlertDialog class.
         This is basically a dialog but sets the background to empty so it can do
         two-tone backgrounds. For applications targeting Honeycomb or newer, this is the default
         AlertDialog theme. -->
    <style name="Theme.AppCompat.Dialog.Alert" parent="@*android:style/Theme.Holo.Dialog.Alert"/>
    <style name="Theme.AppCompat.Light.Dialog.Alert" parent="@*android:style/Theme.Holo.Light.Dialog.Alert"/>

    <!-- Variant of Theme.AppCompat.Dialog that has a nice minimum width for
         a regular dialog. -->
    <style name="Theme.AppCompat.Dialog.MinWidth" parent="@android:style/Theme.Holo.Dialog.MinWidth"/>
    <style name="Theme.AppCompat.Light.Dialog.MinWidth" parent="@android:style/Theme.Holo.Light.Dialog.MinWidth"/>

    <!-- Menu/item attributes -->
    <style name="Theme.AppCompat.CompactMenu" parent="@*android:style/Theme.Holo.CompactMenu"/>
</resources>
